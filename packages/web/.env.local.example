# Anthropic — required for the chat agent
ANTHROPIC_API_KEY=

# WalletConnect — get a free projectId at https://cloud.reown.com (formerly walletconnect.com)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Sepolia RPC — defaults to public Ankr if not set, but rate-limited.
# Use Alchemy/Infura for the live demo.
NEXT_PUBLIC_SEPOLIA_RPC_URL=

# Smart account address that the agent operates on Sepolia.
NEXT_PUBLIC_AGENT_SMART_ACCOUNT_ADDRESS=0x6EB916196e1A081234B26a977DFacF32510fA6C7

# Agent ENS — what the agent identifies itself as.
NEXT_PUBLIC_AGENT_ENS=demo-agent.ensfirewall.eth

# Owner private key for the smart account at NEXT_PUBLIC_AGENT_SMART_ACCOUNT_ADDRESS.
# When set, the chat agent's sendTransaction tool BROADCASTS real transactions on
# Sepolia after offchain policy validation passes. When absent, the tool falls
# back to simulated mode (validates only).
#
# SERVER-ONLY. Do NOT prefix with NEXT_PUBLIC_ — that would expose the key in
# the browser bundle. In Vercel, mark this variable as Sensitive.
#
# Demo cap: each broadcast is limited to 0.0001 ETH (hardcoded in lib/tools.ts).
DEPLOYER_PRIVATE_KEY=
